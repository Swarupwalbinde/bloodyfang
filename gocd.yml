format_version: 2
pipelines:
  pipeline_code_demo:
    group: defaultGroup
    materials:
      backend:
        git: https://github.com/Swarupwalbinde/bloodyfang.git
        branch: main
    stages:
      - Build_Demo_Project_Stage:
          clean_workspace: true
          jobs:
            build_task:
              tasks:
                - exec:
                    run_if: passed
                    command: /bin/bash
                    arguments:
                    - yum install httpd -y
                    
      