format_version: 2
pipelines:
  pipeline_code_demo:
    group: defaultGroup
    materials:
      backend:
        git: https://gitlab.com/Swarupwalbinde/trial2/-/blob/main/assignment.yml
        branch: master
    stages:
      - Build_Demo_Project_Stage:
          clean_workspace: true
          jobs:
            build_task:
              tasks:
                - exec:
                    run_if: passed
                    command: /bin/bash
                    arguments:
                    - pwd
      